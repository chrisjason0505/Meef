# Malware Sentinel — Dependencies
# Install: pip install -r requirements.txt

numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
imbalanced-learn>=0.12.0
joblib>=1.3.0
streamlit>=1.31.0
requests>=2.31.0
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
# note: no google-generativeai SDK needed -- report_agent.py calls the Gemini REST API directly via 'requests'
# note: Groq (cross-provider LLM fallback) also uses plain 'requests' against its
# OpenAI-compatible REST endpoint -- no groq SDK needed. Optional: set GROQ_API_KEY
# to enable it (see README).
